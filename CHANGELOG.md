# 更新日志

## 行为 / 功能 / 素材丰富 · 0.4.0（2026-08-20）

- **时段感知**：凌晨/早晨/白天/夜晚四时段，每天每时段首次 idle 问候（防刷屏）；深夜待机小动作放缓 ×1.7、完工语音音量减半、撒花减量，不扰人。
- **心情系统**：-100~100（localStorage 持久化），摸头 +6、双击 +2、完工 +4、出错 -8，每天 4 点后向 0 回归 20%；≥40 金色光晕爱打招呼、≤-40 灰调爱发呆，跨档补心情气泡。
- **互动扩展**：摸摸头（按住 500ms）、双击惊喜、右键菜单（摸头/戳一戳/打个盹30s/去睡会儿藏60s/切形象/音量）、悬停盯看、打盹（idle 12 分钟自动化）。
- **待机行为链**扩到 8 种（新增打哈欠/望天/摇摆/发呆），带 combo 连招，12% 直接吐心事。
- **素材**：全部文案池扩充 + 心情气泡色（b-happy/b-low）+ 新 CSS 动画（sleepBreath 等）+ v2 新增 sleep track 与 sleepy/surprised/soothe/yawnish/glancey 序列。
- **功能/主进程**：窗口位置记忆（userData）、pet-hide 隐藏 + 定时自动回来 + pet-wake 唤回播报。
- 配套文档：SKILL.md「七、交互与行为」；完整清单见归档 `E:\AI\HANA\03-项目与工作语境\桌宠插件\jiadaizi-pet-行为功能素材丰富-2026-08-20.md`。

## 2.0.0 完整插件化（2026-08-16 深夜）

- **架构升级**：从「独立 Electron + 桥接插件」两件套，改为单一 Hana 插件 `jiadaizi-pet`。插件内嵌桌宠本体（pet/electron）、素材（pet/assets）、事件桥接（pet-core.js）、skill（skills/）与 4 个工具（tools/）。
- **一键安装**：放目录进 plugins 重启即用，随 Hana 启动自动拉起、退出自动回收（keepAlive 可保留常驻）。
- **旧桥接退役**：`whale-pet-bridge` 已卸载，不再需要桌面快捷方式与 vbs 启动链，桌面 `deepseek娘.lnk` 已删除。
- **配置页**：autoStart / port / keepAlive / electronDir 四项，全部在 Hana 设置页可视化调整。

## 语音播报时机修复（2026-08-17 ・ agent_end 根治）

- **根因**：Hana 运行中 `agent_end` 事件时有时无（08-16 夜已实测，不可依赖单一事件）。曾改为单靠 agent_end 触发庆祝，它一缺席，整轮完工就哑火不播报。
- **根治**：庆祝不再依赖 agent_end 的到达。`turn_end` 立即启动静默窗口（2s 无新活动才庆祝），工具间隙的 turn_start 只续期、新活动（session_user_message / agent_start / tool_execution_start / message_start）硬取消，只有真正的整轮结束才会通过静默窗口。agent_end 到来时只是加速确认，不来也照常庆祝。
- **错误容错**：原「本轮任一工具 isError 即整轮沉默」过严（20:02 实测：一轮 8 次成功 + 1 次被审批拒绝的工具 → 全程静音）。改为计数比较：仅当错误数 ≥ 成功数（这轮基本没干成）才不庆祝，被拒工具的 failed 动画与语音提示保留。
- **干过活才响**：纯聊天轮次（本轮无 tool_execution）不撒花不播语音。

## 完工语音修复（2026-08-16 夜・turn_end 判定）

- **（该 turn_end + 静默窗口思路已在 2026-08-17 夜以更完善的形态回归：turn_end 兜底 + 容错计数，见上方「agent_end 根治」条目）**
- **根因**：Hana 运行中不再广播 `agent_end` 事件，桥接插件庆祝逻辑的唯一入口 agent_end 等不到信号，导致完工后既不撒花也不播语音。
- **修复**：庆祝入口从 agent_end 改为 turn_end + 静默窗口判定。turn_end 先回待机，同时启动 2 秒静默确认，期间无新动作（turn_start / tool_execution_start 等）才算真正收工；轮内每个 turn 切换会自己取消，只有整轮收工才撒花 + 语音。
- **双保险**：agent_end 时有时无，turn_end 加静默窗口不依赖单一事件，更抗造。

## 完工语音更换（2026-08-16 傍晚・碧蓝档案佳代子）

- **完工语音从微软 Huihui TTS 换成碧蓝档案角色「佳代子」的日语音色**：TTS-Online 在线生成，佳代子_1 音色，老爷确认“就是这个味”。
- **5 句日文完工语音**（voice-1~5.mp3）：完了しました！お疲れ様です！ / はい、完了です。 / ちゃんとやりましたよ。 / おつかれさま。終わったよ。 / お待たせしました。完了です。
- **旧 Huihui wav 备份**到 assets/backup-wav-20260816/。
- **main.js 语音路由放宽**到 .mp3|.wav（防穿越白名单保留），pet.html VOICES 指向 mp3。
- **中文版尝试失败**：佳代子为日语音色，平台不支持中文朗读，按“优先日文”指示放弃中文版。

## Hana 桥接插件（2026-08-16 夜・庆祝时机 + 思考/工作交替修正）

- **撒花/语音只在整轮结束时触发**：庆祝入口收窄为「整轮结束 + 2 秒静默确认（期间无新动作）+ 本轮做过工具且无错」。多 turn 任务中间不再误庆祝，只在最终等待下一步指示时播报。
- **多段思考每段都显示**：庆祝收窄到整轮结束后，中间段的 turn_start 能正常切入思考态，不再被 4.8 秒庆祝动画吞掉。
- **思考/工作轮流交替**：工具结束从「保持工作中」改为「回到思考」，turn 边界回待机，让下一段 turn_start 重新切入思考态，review 与 working 轮流出现增添节奏。
- **回落时长对齐**：celebrating 回落从 3 秒改为 5.2 秒（略长于桌宠侧 4.8s 动画），failed 保持 3 秒。

## Electron 版（2026-08-16 夜・庆祝后安静待机）

- **celebrating 自动回退 idle**：庆祝完一直跳会心烦，桌宠不该催回复；waiting 改为仅在真需要输入时显式设置。实测：庆祝 4.8s 后落 idle。
- **待机随机小动作去掉蹦跳**：22px 大蹦跶换成“左看看右看看”（runRight→runLeft），只留挥手/歪头/张望这种轻的。

## Electron 版（2026-08-16 晚・相位协议 v2）

- **相位同步协议**：核心“提前一拍驱动”——批末预告下一相位，working/review/celebrating 与实际动作同拍。机械规则：回合首批发 working、批末按下一件事预告（分析→review / 动手→保持 working）、批首从 review 切回 working、最终回答前批末发 celebrating、自动回退不手动复位；驱动命令拼进本来要跑的命令开头，零额外进程。

## Electron 版（2026-08-16 下午・三态重做）

- **完工语音换 TTS**：旧截取音（4s长/底噪/单曲循环）废弃，改 Microsoft Huihui 合成 5 句短语音（voice-1..5.wav，SSML pitch+18% rate+12%，1.3~3s），每次庆祝随机一句；新增路由 `/voice-N.wav`（白名单正则防穿越）。
- **干活态重做**：匀速微颤 → 打字顿挫节奏（三连哒哒哒+停顿，1.15s 周期，前倾 1.5deg）。
- **思考态重做**：小幅歪头 → 大幅缓慢左右倾 ±3.4deg（4.4s），气泡换“唔…让我想想”+ 三个绿点依次浮现动画（dots 只在 review 态显示）。
- **庆祝态重做**：跳跃加蓄力拉伸→22px 腾空→落地压扁（squash & stretch，660ms）；新增撒花粒子（头顶扇形抛出 16 粒+后续补投，六色，抛物线+旋转+淡出）；节奏放缓为跳 700ms/摇 940ms 交替；窗口加高 260→300 给粒子留空。
- **IPC 即时推送**：主进程 setMode 时即时推送，页面 preload onMode 即时切换；轮询从 400ms 降为 2s 兜底。状态切换延迟从最多 400ms → 几乎即时。

## Electron 版（2026-08-16 上午）

- **动起来**：全部状态加 CSS transform 微动效——idle 漂浮+呼吸、working 打字颤动、review 歪头晃悠、waiting 期待小跳、failed 泄气下沉、celebrating 跳跃弧线+挥手摇摆、拖拽挣扎抖动；新增地面阴影（随浮动反向缩放），气泡换文案时弹跳 pop，入场淡入放大。
- **待机随机小动作**：每 14~30 秒自己挥挥手/蹦跶/歪头/眼巴巴看你，2.4 秒后回待机。
- **热重载**：新增 `POST /jiadaizi-pet/reload`（win.reload），改 pet.html 后无需重启进程。
- **无黑窗启动链**：HanaAgent.lnk → start-hana-with-pet.vbs；deepseek娘.lnk → start-pet.vbs（wscript 启动，不再弹 cmd 窗）。
- **素材澄清**：精灵图实际为 9 个单帧动作（画布左列一列），早期“8×9 网格”是误读，动态全靠程序变换实现。
